# ملاحظات مراجعة نسخة ويندوز — 2026-09-25

- نسخة ويندوز الرسمية مبنية عبر **Electron** من نفس واجهة React المشتركة؛ لذلك إصلاحات الفواتير والديون والطباعة والسجل الزمني تطبّق في ويندوز تلقائياً ولا توجد واجهة أعمال منفصلة قد تختلف عنها.
- النسخة المنتجة هي مثبت NSIS ونسخة محمولة x64 لـ Windows 10/11؛ كلاهما يغلّفان ملف `dist` نفسه، ويُفحص وجود الواجهة وملفات Electron داخل `app.asar` في CI.
- سبب التحسين: قنوات Electron التي تحفظ الملفات وتطبع وتعرض الإشعارات كانت تحتاج تحققاً دفاعياً صريحاً من مصدر طلب IPC، وتحقق اسم الملف كان يعتمد على فاصل مسارات النظام فقط.
- تم قصر جميع قنوات IPC ذات الصلاحية على صفحة التطبيق الموثوقة، وقصر خادم التطوير على `localhost:5173`، ومنع فواصل مسار Windows وPOSIX والأسماء غير الصالحة.
- تم استبدال الملف المؤقت ذي الاسم الثابت بملف مؤقت فريد مع تنظيف مضمون؛ يمنع تعارض عمليتي حفظ متزامنتين ولا يترك ملفاً جزئياً بعد الفشل.
- تم تفعيل CSP للواجهة من دون `unsafe-inline` للسكربتات، ونقل تهيئة السمة إلى ملف مستقل؛ تبقى المصادر محلية ويستمر بدء التطبيق بالسمة الصحيحة.
- بقيت خصائص ويندوز العملية متطابقة مع التطبيق: حفظ أصلي عبر مربع Windows، طباعة عبر `webContents.print`، إشعارات النظام، بيانات محلية IndexedDB، نسخة وحيدة، اختصارات وقائمة سياق عربية، وتكبير محفوظ.
- التحقق المنفذ محلياً: `npm run verify` نجح (303 اختباراً)، وتدقيق الأغلفة نجح بـ 112 فحصاً، و`npm audit --audit-level=high` لم يجد ثغرات.
- التحقق التنفيذي على Windows 10/11 موجود في CI: بناء المثبت والمحمول، تشغيل اختبار دخان لكليهما، ثم تثبيت/إزالة صامتان والتحقق من بقاء بيانات المكتب.
