# تقرير التدقيق الشامل — نظام إدارة المكتب الزراعي

تاريخ التدقيق: 2026-09-19
النطاق: كامل المستودع (طبقة البيانات، واجهات الصفحات، الطباعة/PDF، النسخ الاحتياطي، أغلفة سطح المكتب وأندرويد، البناء وCI).

الملخص: **28 مشكلة** تم إصلاحها، منها 7 كانت تسبب تلف بيانات مالية أو توقف التطبيق، و4 ثغرات أمنية.
كل إصلاح مُغطّى باختبار آلي (52 اختباراً) — راجع `tests/`.

---

## 1) أخطاء حرجة تُتلف البيانات المالية

| # | المشكلة | الأثر قبل الإصلاح | الإصلاح |
|---|---------|-------------------|---------|
| 1 | حفظ الفاتورة لم يكن داخل معاملة (transaction) | انقطاع أثناء الحفظ = فاتورة بلا بنود أو مخزون غير متزن | `src/lib/invoices.ts` — `saveInvoice()` داخل `db.transaction('rw', …)` |
| 2 | **تسريب مخزون عند فشل تعديل فاتورة**: كان الكود يحذف بنود الفاتورة القديمة ثم يحاول "إرجاع" الكميات بقراءة البنود المحذوفة (فارغة دائماً) | كل محاولة تعديل فاشلة كانت تضيف كميات وهمية للمخزن (+10 في الاختبار) | التحقق من المخزون **قبل** أي كتابة، وحساب الفروق (deltas) وتطبيقها دفعة واحدة |
| 3 | حذف فاتورة كان يُرجع الكميات خارج المعاملة | فشل الحذف = مخزون مُضخّم | `deleteInvoice()` داخل معاملة واحدة |
| 4 | **أرقام فواتير ووصولات مكررة** (`count() + 1`) | بعد حذف أي فاتورة يُعاد استخدام رقمها — خلل في سجل رسمي | `src/lib/sequence.ts` — أعلى رقم مستخدم ∪ علامة high-water في جدول `meta` + فحص تفرد |
| 5 | `remaining` على الفاتورة لا يتحدث بعد التسديد | قائمة الفواتير تعرض مبالغ متبقية خاطئة | `reallocateCustomerInvoices()` يوزّع المسددات على أقدم الفواتير بعد كل عملية |
| 6 | دفعة المقدمة على فاتورة آجلة لم تُسجَّل كقبض | تقرير "حركة الصندوق" أقل من الواقع | تُنشأ كسجل `Payment` بمصدر `downpayment` داخل نفس المعاملة |
| 7 | الخصم والمدفوع غير مقيّدين | إجمالي سالب / دين سالب | تقييد `0 ≤ discount ≤ subtotal` و `0 ≤ paid ≤ total` |

## 2) انهيارات وتوقف التطبيق

| # | المشكلة | الأثر | الإصلاح |
|---|---------|-------|---------|
| 8 | `new Date('' + 'T23:59:59').toISOString()` في التقارير | تفريغ حقل التاريخ يرمي `RangeError: Invalid time value` فتتوقف كل التقارير بصمت | `localDayRangeISO()` تُعيد `null` + رسالة للمستخدم (`src/pages/Reports.tsx`) |
| 9 | لا معالجة للأخطاء غير الملتقطة | أي فشل في IndexedDB = واجهة فارغة بلا رسالة | `src/lib/errors.ts` (window.onerror + unhandledrejection) + نظام تنبيهات `src/lib/toast.ts` |
| 10 | كل استدعاءات قاعدة البيانات في `useEffect` بلا `catch` | تجمّد البيانات على أصفار | توحيد المعالجة عبر `reportError()` في كل الصفحات |
| 11 | لا فحص لتوفر IndexedDB | وضع التصفح الخاص = شاشة بيضاء | `checkStorageAvailable()` + شاشة عربية واضحة (`src/App.tsx`) |
| 12 | `orderBy('createdAt')` يتجاهل السجلات بلا مفتاح | فواتير قديمة/مستوردة تختفي من لوحة التحكم | دالة ترقية v3 تستكمل `createdAt`/`status`/`remaining`/`quantity` |
| 13 | جدولا الإشعارات وسجل النشاط ينموان بلا حد | تباطؤ تدريجي مع مرور السنين | `pruneHistory()` يومي (500 نشاط / 300 إشعار) |
| 14 | `getAllCustomersDebt()` تستعلم مرتين لكل زبون | بطء ملحوظ مع فواتير سنة كاملة | `getCustomerBalances()` بمرور واحد على الجدولين |
| 15 | الحزمة الرئيسية 827KB | إقلاع بطيء على الأجهزة الضعيفة | تقسيم الحزم → 224KB + حزم منفصلة (react/dexie/pdf/icons) |

## 3) ثغرات أمنية

| # | الثغرة | الإصلاح |
|---|--------|---------|
| 16 | **XSS مخزّن**: أسماء الزبائن/المواد/الملاحظات تُحقن خاماً في `document.write()` عند الطباعة — ويمكن حقنها عبر ملف نسخة احتياطية مستورد | `escapeHtml()` على كل قيمة + بناء HTML في `src/lib/print.ts` (مُختبر بـ DOMParser) |
| 17 | **استيراد نسخة احتياطية بلا تحقق** — أي ملف يمسح البيانات وقد يُدخل قيماً تكسر التطبيق | `src/lib/validate.ts`: تحقق من البنية، تطهير الأنواع، تجاهل السجلات التالفة مع تحذيرات |
| 18 | رموز الدخول (PIN) نص صريح وتظهر في الواجهة | `src/lib/security.ts` — SHA-256 + ملح، وعرض `••••`، وترقية تلقائية للرموز القديمة |
| 19 | Electron: روابط خارجية تتنقل داخل النافذة، بلا قفل نسخة وحيدة، بلا `sandbox` | `electron/main.cjs` — `setWindowOpenHandler` + `will-navigate` + `requestSingleInstanceLock` + `sandbox: true` + كتابة ذرّية للملفات + منع اجتياز المسار |
| 20 | Capacitor: `allowMixedContent: true` | تعطيله + `androidScheme: https` |

## 4) النسخ الاحتياطي

| # | المشكلة | الإصلاح |
|---|---------|---------|
| 21 | النسخ التلقائي يُنزّل ملفاً كل ساعة (ويفتح صندوق حفظ في ويندوز) كل فترة | نسخ داخلية في IndexedDB (آخر 5) + استعادة بضغطة، والتصدير يبقى يدوياً |
| 22 | `beforeunload` يكتب النسخة الكاملة في `localStorage` | حُذف — كان يتجاوز الحصة ويرمي خطأً ولا يُقرأ أبداً |
| 23 | `setInterval` بلا تنظيف ويتكرر في StrictMode | `setupAutoBackup()` وحيد ويُعيد دالة إيقاف تُستدعى عند إلغاء التركيب |
| 24 | الاستيراد لا يُعيد بناء الأرصدة | `restoreBackupData()` تُصالح الأرصدة وتضمن وجود إعدادات |

## 5) صحة الحسابات والتواريخ

| # | المشكلة | الإصلاح |
|---|---------|---------|
| 25 | `toISOString().slice(0,16)` لملء `datetime-local` (UTC بدل المحلي) | `formatLocalDateTimeInput()` — فرق 3 ساعات في العراق كان يُظهر اليوم السابق |
| 26 | حدود "اليوم" في التقارير محسوبة بتوقيت UTC | `startOfDayLocal/endOfDayLocal/localDayRangeISO` |
| 27 | لا تقريب للمبالغ | `roundMoney()` في كل المسارات المالية (0.1 + 0.2 ≠ 0.3) |
| 28 | القيم النصية في السجلات القديمة تكسر الحسابات | `toFiniteNumber()` + ترقية v3 + تطهير عند الاستيراد |

---

## البنية الجديدة

```
src/lib/
├── db.ts            المخطط والترقيات (v1 → v2 → v3) والإعدادات والإشعارات
├── invoices.ts      حفظ/تعديل/حذف الفواتير داخل معاملات + توزيع المسددات
├── payments.ts      التسديدات + إعادة حساب الأرصدة
├── debts.ts         حساب الأرصدة بمرور واحد
├── sequence.ts      أرقام فواتير/وصولات فريدة
├── backup.ts        تصدير، نسخ داخلية، استعادة
├── validate.ts      التحقق من ملفات النسخ الاحتياطية
├── print.ts         طباعة آمنة (HTML مُهرَّب)
├── pdf.ts           تصدير jsPDF
├── maintenance.ts   صيانة بدء التشغيل (مصالحة + قص السجلات + ترقية PIN)
├── security.ts      بصمات رموز الدخول
├── platform.ts      أنواع جسور Capacitor/Electron
├── errors.ts        معالجة مركزية للأخطاء
├── toast.ts         تنبيهات داخل التطبيق
└── utils.ts         أموال/تواريخ محلية/تهريب HTML
```

## أوامر التحقق

```bash
npm run lint        # ESLint 9 (flat config)
npm run typecheck   # tsc --noEmit (strict)
npm test            # 52 اختباراً (Vitest + fake-indexeddb + Testing Library)
npm run build       # typecheck + vite build (PWA)
npm run verify      # الأربعة معاً
```

الاختبارات تغطي: خصم المخزون، فشل التعديل بلا تسريب، حذف الفاتورة، تفرد الأرقام،
توزيع المسددات (FIFO)، دفعة المقدمة، حساب الأرصدة، تصدير/استيراد النسخ، رفض الملفات
التالفة، الترقية من v2، تهريب HTML في الطباعة، وإقلاع التطبيق فعلياً في المتصفح.
