# تقرير التدقيق — الجولة الثانية (البنود المتبقية)

تاريخ الجولة: 2026-09-23
النطاق: البنود الستة المتبقية من التدقيق الشامل بعد دمج الجولة الأولى.
حالة التحقق في هذه الجولة: `npm run verify` **ناجح** (تدقيق الأغلفة 77/77، ESLint 0 أخطاء،
`tsc --noEmit` نظيف، **143 اختباراً في 14 ملفاً**، بناء الإنتاج ناجح بلا تحذيرات)،
و`npm audit` = **0 ثغرات** (كان 24: 6 متوسطة، 15 عالية، 3 حرجة) — **دون** استخدام
`npm audit fix --force`.

## إصلاحات كشفها التحقق من النواتج الفعلية في CI

أول تشغيل كامل لخط البناء كشف ثلاثة أعطال حقيقية لا يظهرها التدقيق الثابت:

| العطل | السبب الجذري | الإصلاح |
|------|---------------|---------|
| فشل بناء APK: `Found item String/app_name more than one time` | سكربت التجهيز كان يعتبر الوسم غائباً فيُضيف نسخة ثانية من `app_name` | `syncAppName()` يُزامن الموجود ويدمج التكرار في نسخة واحدة + 11 اختباراً تشغّل السكربت فعلياً على مشروع مؤقت وتتحقق من التكرارية |
| فشل التحقق من محتوى `app.asar` على ويندوز | `@electron/asar list` يطبع الفواصل بنمط النظام (`\`) فلم تُطابق المسارات | توحيد الفواصل قبل المقارنة + طباعة أول المدخلات للتشخيص |
| فشل التحقق من حزمة Tauri على لينكس | التحقق بحث عن ملفات الواجهة داخل الحزمة، بينما Tauri يدمج الواجهة داخل الملف التنفيذي | التحقق الصحيح: نسخة `dist` + محتوى الحزمة + وجود `id="root"` داخل الملف التنفيذي + سلامة الـ AppImage |

كما أصبح اختبار الدخان في Electron ينتظر اكتمال الإقلاع وإنشاء قاعدة البيانات
(`data-app-boot` + `indexedDB.databases()`) بدل الاكتفاء برسم الواجهة، وهذا ما
يجعل تحقق ويندوز من بقاء البيانات بعد إلغاء التثبيت ذا معنى.

---

## 1) اختبار واجهة حقيقي لزر الرجوع و Escape

| الحالة | التغطية |
|--------|---------|
| زر الرجوع في أندرويد مع نافذة مفتوحة | `tests/backNavigation.test.tsx` — يُغلق النافذة ولا يغادر الصفحة ولا يُستدعى `exitApp` |
| زر الرجوع بلا نوافذ | يرجع شاشة واحدة داخل التطبيق |
| زر الرجوع من الصفحة الرئيسية | يبقى في التطبيق ويظهر تنبيه «أنت في الصفحة الرئيسية» ولا يخرج إطلاقاً |
| زر الرجوع في Electron/Tauri/المتصفح (حدث السجل) | يغلق النافذة ولا يغيّر المسار، وفخّ السجل يُستهلك مرة واحدة لكل رجوع حقيقي |
| Escape مع نوافذ متعددة | يُغلق الطبقة العليا فقط (LIFO) ثم التي تحتها |
| الضغط المتكرر على Escape | لا يفعل شيئاً بعد إغلاق كل الطبقات ولا يغيّر المسار |
| إزالة المستمعين عند إلغاء التركيب | لا تسريب مستمعين (اختبار صريح) |

أُضيفت هذه الجولة اختبارات لمنطق الرجوع المجرّد والفخّ
(`tests/lifecycle.test.ts` — 3 اختبارات): تسليح مدخل واحد فقط، استهلاك الرجوع
الحقيقي، وإعادة التسليح **فقط** إذا بقيت طبقات أخرى، وعدم التدخل في الرجوع العادي.

## 2) اسم المكتب الطويل: من الاقتطاع إلى الجذر الحقيقي

- الاسم يُطبَّع (فراغات/أسطر) بلا اقتطاع أي حرف، وحدّه 150 حرفاً مع رسالة واضحة.
- يظهر كاملاً: في الشريط الجانبي (بلا `truncate` + `title`)، وشاشة الإعدادات،
  وترويسة الفاتورة والوصل وكشف الحساب (تصغير الخط للأسماء الطويلة 22/19/16px).
- في اسم ملف النسخة الاحتياطية وحده يُقتطع بميزانية **بايتات** (UTF-8, 120 بايت)
  مع الحفاظ على الطابع الزمني والامتداد: `…_Backup_2026-05-03_09-08-07.json`.
- **السبب الجذري لأحد تذبذبات الاختبار** كان اعتماد التخطيط على `useLiveQuery`
  وحده لعرض الاسم؛ أُضيف مخزن إعدادات مشترك (`src/lib/settingsStore.ts` +
  `useOfficeSettings`) يُنشَر عند الحفظ وعند كل قراءة، فيظهر الاسم المحفوظ في
  **أول رسم**. القياس بعد الإصلاح: الاسم يظهر في **0–1 مللي ثانية** بدل انتظار
  إطار كامل، والاختبار نجح **10/10** تشغيلات متتالية.

## 3) بناء الأغلفة الحقيقية (APK / مثبّت ويندوز / Tauri)

البناء المحلي غير ممكن في هذه البيئة (لا JDK/Android SDK ولا Wine/NSIS ولا Rust،
والشبكة تحجب `dl.google.com` و`static.rust-lang.org` و`objects.githubusercontent.com`)،
فنُقل التحقق إلى طبقتين:

1. **تدقيق ثابت شامل** — `npm run audit:shells` (73 فحصاً): أمان نافذة Electron
   (contextIsolation/sandbox/preload/منع النوافذ/allowlist للأذونات)، نسخة واحدة من
   التطبيق، مجلد بيانات ثابت للنسخة المحمولة، كتابة ذرّية للملفات، أهداف NSIS/portable،
   إعداد Capacitor (allowMixedContent=false، أيقونة الإشعارات، صلاحيات الملفات)،
   صلاحيات Tauri وأيقوناته، وناتج الويب (مسارات نسبية تعمل تحت `file://`).
2. **تحقق على النواتج الفعلية في CI** (`.github/workflows/build.yml`):
   - **أندرويد**: `prepare-android.mjs` (idempotent، يُختبر بتنفيذه مرتين و`diff`)،
     فحص `targetSdk ≥ 33` و`compileSdk ≥ 34`، بناء `assembleRelease`، توقيع، ثم
     `aapt2 dump permissions/resources` و`unzip` للتحقق من `POST_NOTIFICATIONS`
     و`READ_EXTERNAL_STORAGE` وأيقونة `ic_stat_agri` و`fileprovider` وواجهة `assets/public`،
     مع التأكد من أن `app_name` معرّف **مرة واحدة** بالضبط (تكراره يُفشل Gradle).
   - **ويندوز**: بناء NSIS + portable، فحص محتوى `app.asar`، **تشغيل النسخة المحمولة**
     واختبار دخان فعلي، **تثبيت المثبّت صامتاً** (`/S`) وتشغيل النسخة المثبّتة، ثم
     إلغاء التثبيت والتحقق من أن بيانات المكتب (IndexedDB) **بقيت** بعد إلغاء التثبيت.
   - **Tauri**: بناء حزم deb/AppImage على لينكس وmsi/nsis على ويندوز، والتحقق من
     نسخ الواجهة داخل الحزمة (`dpkg-deb -c … dist/index.html`).

## 4) تنظيف تحذيرات قاعدة البيانات

- `lifecycle.ts`: علامة إغلاق واحدة + نطاقات إلغاء + تصنيف أخطاء، ووسيط DBCore يمنع
  أي استعلام جديد بعد الإغلاق.
- أُصلح التصنيف ليشمل الأخطاء الملفوفة (`error.inner`) والأخطاء غير الممتدة من `Error`
  (مثل `DexieError` من عالم مختلف) — كانت تُصنَّف عطلاً فيظهر تحذير مزعج.
- `reportError` لم يعد يطبع شيئاً للأخطاء المتوقعة (إغلاق/مغادرة شاشة)، و`logBackgroundFailure`
  يطبع التحذير للعطل الحقيقي فقط.
- **النتيجة المُقاسة**: مخرجات `vitest run` كاملة (13 ملفاً) **بلا أي `Warning:` ولا
  `DatabaseClosedError`** — تحقُّق آلي عبر `grep` على سجل التشغيل، مع 15 اختباراً
  في `tests/lifecycle.test.ts` تغطي الإغلاق والنطاقات والفخّ وقرار الرجوع.

## 5) مراجعة الاعتماديات

- `npm audit`: من 24 ثغرة إلى **0**، بمسار `overrides` جراحي واحد
  (`xcode → uuid@^11`) وبلا `--force` إطلاقاً.
- الاعتماديات الأصلية المتخطاة سابقاً (Capacitor) ثُبّتت باصدارات موجودة فعلاً.
- **ترقية Vite 8 كشفت عطلاً حقيقياً في البناء**: `manualChunks` بصيغة كائن لم تعد
  مدعومة، ففشل `vite build`. أُصلح جذرياً في `vite.config.ts` (دالة تقسيم + مسار
  `import.meta.dirname`)، وأُزيل استيراد ديناميكي غير فعّال في `nativeBridge.ts`
  كان يُنتج تحذير Vite، مع توثيق حدّ تحذير حجم الحزمة لأن أكبرها (مولّد PDF) مُحمَّل تأخيرياً.

## 6) اختبار التدفقات الحساسة

| التدفق | التغطية |
|--------|---------|
| `/payments/new` (قبض) | مسار مستقل + عنوان ونموذج بلا إعادة تهيئة؛ `tests/payments.test.ts` + اختبارات الحالة المفتوحة |
| حذف كل البيانات ثم معالج الإعداد | `clearAllData` + `refreshSettings()` ⇒ المخزن يعود فارغاً ويظهر المعالج؛ تغطية في `tests/sensitiveFlows.test.ts` |
| استيراد نسخة تالفة | رفض بلا أي مساس بالبيانات (`normalizeBackup`/`readBackupFile`) |
| استيراد نسخة ناقصة/مبتورة | **حماية جديدة**: نسخة بلا أي سجل تجاري لا تمحو بيانات مكتب قائم (ترمي خطأً واضحاً) |
| تعديل فاتورة مدفوعة | لا تُفقد التسديدات، ولا تُنشأ دفعة مقدمة وهمية، والمخزون متوازن، والدفع الزائد يظهر رصيداً دائناً للزبون |
| نقل فاتورة وتسديداتها بين زبونين | الفحص داخل معاملة واحدة: الدين كامل على الزبون الجديد ولا أثر على القديم |
| حذف مادة/زبون له سجلات مالية | مرفوض داخل معاملة (بعد تحويل القاعدة من الشاشة إلى `src/lib/customers.ts` و`materials.ts` القابلين للاختبار) |
| التقارير بمدى تواريخ متغير بسرعة | تأخير الطلبات + `reportRequestRef` يمنع نتائج قديمة من الكتابة على واجهة أحدث، والمدى غير الصالح رسالة مشتقة بلا `RangeError` |

## حمايات وإصلاحات إضافية في هذه الجولة

- **HTML غير صالح**: `<Badge>` (عنصر `div`) كان داخل `<p>` في صفحة النسخ الاحتياطي
  (تحذير React DOM nesting + إعادة ترتيب شجرة المتصفح) ⇒ صار `<div>`.
- **`README`-level**: `.gitignore` كان يستثني `resources/android/` (بسبب نمط `android`)
  فيختفي أصل أيقونة الإشعارات من CI ⇒ أُضيف استثناء صريح.
- **قابلية الاختبار**: أُضيفت `tests/customers.test.ts` و`tests/sensitiveFlows.test.ts`
  و`tests/lifecycle.test.ts` (38 اختباراً جديداً).

## ما يبقى تحققاً يدوياً على جهاز حقيقي (لا يمكن محاكاته هنا)

1. تثبيت APK على أندرويد 13+: طلب إذن الإشعارات، وصول إشعار فعلي، مشاركة ملف
   PDF/نسخة احتياطية عبر درج المشاركة، وسلوك زر الرجوع الأصلي أثناء فتح نافذة.
2. تشغيل مثبّت ويندوز: حفظ PDF عبر صندوق الحفظ الأصلي، النسخ الاحتياطي، وفتح
   الروابط الخارجية في المتصفح (لا داخل النافذة).
3. حزمة Tauri: التشغيل، النسخة الواحدة (نقرتان ⇒ نافذة واحدة)، والإشعارات.

كل هذه المسارات مُغطّاة بفحوص CI على النواتج الفعلية كما في البند 3، وأي فشل فيها
يظهر كفشل مهمة في خط البناء لا كخطأ صامت.
