# 🌾 الجولة الثانية من التدقيق الشامل — البنود المتبقية

يكمل هذا الـ PR العمل المدمج سابقاً ويعالج **البنود الستة المتبقية** من التدقيق،
بإصلاحات جذرية (لا ترقيع) واختبارات آلية تغطي كل مسار حساس.
التقرير التفصيلي: [`AUDIT-ROUND2.md`](AUDIT-ROUND2.md).

## ✅ حالة التحقق (آخر تشغيل على هذا الفرع)

| الفحص | النتيجة |
|-------|---------|
| `npm run audit:shells` | **82/82 ناجح، 0 تحذير، 0 فشل** |
| `npm run lint` (ESLint 10، بلا أي `eslint-disable` مضاف) | **0 أخطاء، 0 تحذيرات** |
| `npm run typecheck` | **نظيف** |
| `npm test` | **149 اختباراً في 14 ملفاً — كلها ناجحة** |
| `npm run build` | **ناجح بلا تحذيرات** |
| `npm audit` | **0 ثغرات** (كان 24: 6 متوسطة، 15 عالية، 3 حرجة) — بلا `--force` |

## 1) واجهة فعلية للرجوع و Escape

- 11 اختباراً في `tests/backNavigation.test.tsx` + 3 اختبارات منطق في `tests/lifecycle.test.ts`:
  زر رجوع أندرويد مع/بلا نافذة، البقاء في الرئيسية بلا خروج، رجوع السجل في
  Electron/Tauri/المتصفح، Escape مع طبقات متعددة (LIFO)، تكرار Escape، وإزالة المستمعين.

## 2) اسم المكتب الطويل — إصلاح السبب الجذري

- الاسم يُحفظ كاملاً (حد 150 حرفاً، تطبيع بلا اقتطاع) ويظهر كاملاً في التخطيط
  والفواتير والسجلات، ويُقتطع فقط في **اسم ملف** النسخة الاحتياطية بميزانية بايتات UTF-8.
- **السبب الجذري للتذبذب**: كان العرض يعتمد على استعلام Dexie الحيّ وحده. أُضيف مخزن
  إعدادات مشترك (`src/lib/settingsStore.ts` + `hooks/useOfficeSettings.ts`) يُنشَر عند
  الحفظ وعند كل قراءة ⇒ الاسم يظهر في أول رسم (قياس: 0–1 مللي ثانية، نجاح 10/10).

## 3) بناء الأغلفة الحقيقية

- `scripts/prepare-android.mjs` (idempotent، مُختبر بـ `diff` في CI) + فحوص جديدة في
  `.github/workflows/build.yml`: فحص `targetSdk/compileSdk`، موارد الأيقونة داخل APK،
  تشغيل النسخة المحمولة على ويندوز، تثبيت صامت وتشغيل النسخة المثبّتة، والتحقق من
  بقاء بيانات المكتب بعد إلغاء التثبيت، والتحقق من الواجهة داخل حزمة Tauri.
- `.gitignore`: أُضيف استثناء لأصل أيقونة الإشعارات `resources/android/` (كان مستثنى
  بنمط `android` فيغيب عن CI).

## 4) تنظيف تحذيرات قاعدة البيانات

- تصنيف أخطاء الإغلاق صار يشمل الأخطاء الملفوفة (`inner`) وغير الممتدة من `Error`.
- `reportError` صار صامتاً للأخطاء المتوقعة (إغلاق/مغادرة شاشة)، و`logBackgroundFailure`
  يطبع التحذير للعطل الحقيقي فقط.
- **النتيجة**: تشغيل الاختبارات الكامل بلا أي `Warning:` أو `DatabaseClosedError`.

## 5) الاعتماديات

- 24 → **0** ثغرة عبر `overrides` جراحي واحد، وإصلاح ترقية Vite 8:
  `manualChunks` أصبح دالة + `import.meta.dirname`، وإزالة استيراد ديناميكي غير فعّال
  في `nativeBridge.ts` (كان يُفشل البناء/يُنتج تحذيراً).

## 6) التدفقات الحساسة

- 38 اختباراً جديداً: `tests/customers.test.ts`، `tests/sensitiveFlows.test.ts`،
  `tests/lifecycle.test.ts` — تعديل فاتورة مدفوعة، نقل فاتورة بين زبونين، حذف
  مادة/زبون له سجلات مالية، استيراد نسخة تالفة/ناقصة، حذف كل البيانات والعودة
  لمعالج الإعداد.
- **حماية جديدة**: استيراد نسخة بلا أي سجل تجاري لم يعد يمحو بيانات مكتب قائم.
- تحويل قواعد الزبائن إلى طبقة عمل قابلة للاختبار (`src/lib/customers.ts`) والفحص
  داخل معاملة واحدة (لا فجوة بين الفحص والحذف).
- إصلاح HTML غير صالح: `<Badge>` (div) داخل `<p>` في صفحة النسخ الاحتياطي ⇒ `<div>`.

## 🔧 إصلاحات كشفها التحقق من النواتج الفعلية في CI

- **APK لم يكن يُبنى**: `MergeResources` رفض المانيفست لأن سكربت التجهيز كان يُضيف
  وسم `app_name` بدل مزامنة الموجود ⇒ أُعيدت كتابة المزامنة (تحديث الموجود + دمج أي
  تكرار في نسخة واحدة) مع 11 اختباراً تشغّل السكربت فعلياً على مشروع مؤقت.
- **ويندوز**: توحيد فواصل مسارات `asar list` (يطبع `\` على ويندوز) قبل المقارنة.
- **Tauri**: التحقق من الواجهة داخل الملف التنفيذي (`id="root"`) لأن Tauri يدمج
  الواجهة في الثنائي، بدل البحث عن ملفات داخل الحزمة.
- **اختبار الدخان** في Electron/ويندوز صار ينتظر اكتمال الإقلاع وإنشاء قاعدة
  البيانات (`data-app-boot` + `indexedDB.databases()`) لا مجرد رسم الواجهة.
- **مشاركة الملفات على أندرويد كانت معطّلة**: `@capacitor/share` يحتاج مزوّد ملفات
  بسلطة `${applicationId}.fileprovider` ولم يكن معلناً ⇒ أُضيف إلى `prepare-android.mjs`
  مع `resources/android/file_paths.xml` (مصدر وحيد للحقيقة)، وتغطيه اختبارات
  المشروع المؤقت + تحقق CI من الـ APK المبني.

## 📁 أهم الملفات

**جديد**: `src/lib/settingsStore.ts`، `src/hooks/useOfficeSettings.ts`،
`src/lib/customers.ts`، `tests/customers.test.ts`، `tests/sensitiveFlows.test.ts`،
`tests/lifecycle.test.ts`، `AUDIT-ROUND2.md`
**معدّل**: `src/lib/{db,backup,lifecycle,errors,nativeBridge,invoices,materials,payments,purchases,maintenance,utils}.ts`،
`src/pages/*`، `src/components/layout/Layout.tsx`، `scripts/{audit-shells,prepare-android}.mjs`،
`vite.config.ts`، `.github/workflows/build.yml`، `tests/{setup.ts,officeName.test.tsx}`، `.gitignore`

## ⚠️ ما يبقى تحققاً يدوياً (بيئة/جهاز حقيقي)

تثبيت APK على أندرويد 13+ (إشعارات، مشاركة ملف، زر رجوع أصلي)، تشغيل مثبّت ويندوز
(حفظ PDF، نسخ احتياطي، روابط خارجية)، وحزمة Tauri (نسخة واحدة، إشعارات).
كل هذه المسارات تُبنى وتُفحص آلياً في CI على النواتج الفعلية، والخطوة الأخيرة
تبقى تأكيداً بشرياً على جهاز.
